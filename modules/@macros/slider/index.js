// Provides markup, a Nunjucks helper and styles for a simple slider,
// used for slider both on the front end and the back end.

module.exports = {
  options: { alias: 'slider' },
  helpers(self) {
    return {
    };
  }
};
